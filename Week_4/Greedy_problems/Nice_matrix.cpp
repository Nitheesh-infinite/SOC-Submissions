// url : https://codeforces.com/contest/1422/problem/B

#include<bits/stdc++.h>
using namespace std;

#define ll long long

ll make_palindrome(vector<vector<ll>> v, int m, ll& k){
    
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t;
    cin >> t;
    while(t--){
        int n,m;
        ll k = 0;
        cin>> n>>m;
        vector<vector<ll>> matrix(n, vector<ll>(m, 0));
        for(int i=0;i<n;i++){
            for(int j=0;j<m;j++){
                cin >> matrix[i][j];
            }
        }
        while()

    }
}